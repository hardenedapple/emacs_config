;;; Settings regarding organisation
(setq calendar-week-start-day 1)

(setq calendar-latitude 54)
(setq calendar-longitude 0)

;;; Diary settings
(setq diary-file "~/.emacs.d/diary")
(calendar-set-date-style 'european)

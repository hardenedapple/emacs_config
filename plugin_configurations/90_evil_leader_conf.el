;; Evil-leader (i.e. mapleader for evil)
;; Has to be initialised before evil-mode so is available in *scratch*,
;; *Messages* etc (i.e. in initial buffers)
(global-evil-leader-mode)
(evil-leader/set-leader "<SPC>")
